files:
  - source: '*.csv'
    delimiter: ';'

settings:
  checks:
    # You can pass check types
    - structure
    # ...or individual checks
    - no-headers
    - blank-headers
  error_limit: 1
  table_limit: 1
  row_limit: 5000
  infer_schema: True
  infer_fields: True
  order_fields: True

